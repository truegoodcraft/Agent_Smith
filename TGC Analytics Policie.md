# TGC Analytics Policy

## Purpose

Clarify the intent, scope, and domain for each type and level of online and app analytics used by True Good Craft.

This document exists to:

* Define what analytics means across TGC.
* Establish consistent language.
* Separate useful analytics from unnecessary collection.
* Support product, marketing, and operational decisions without drifting into excessive or unjustified data collection.

## Goal

The goal of analytics is to provide TGC with useful and actionable information for improving products, outreach, distribution, reporting, and operations.

Analytics should be:

* Useful
* Intentional
* Minimal
* Privacy-aware
* Justified by operational or product value

TGC does **not** collect unnecessary personal information just because it is available. Information should only be collected when it helps improve the product, understand usage, evaluate outreach, or support a defined operational need.

## Core Principles

* **Collect with intent:** Every field or signal should have a reason to exist.
* **Prefer minimal collection:** If less data can achieve the same result, use less data.
* **Respect user agency:** Users should not be profiled beyond what is operationally necessary.
* **Separate layers clearly:** Host, page, app, and user-supplied data are not the same thing and must not be confused.
* **Avoid unnecessary PII:** Personal data should not be gathered unless it is explicitly required for a product, service, or user relationship.
* **Use analytics to improve decisions:** The point is better product judgment, not vanity metrics.
* **Privacy-first by default:** Privacy-preserving operation is preferred over deeper collection.
* **Aggregate-only where possible:** If daily or grouped totals are enough, event-level storage should be avoided.
* **No user identifiers without explicit justification:** Install IDs, device IDs, account IDs, cookies, fingerprinting, or similar identifiers should not be introduced casually.
* **No raw IP retention without explicit justification:** IP data should not be stored or repurposed as a uniqueness mechanism unless a system explicitly requires it and the policy says so.
* **No behavioral profiling by default:** TGC analytics should not drift into cross-day user linking or user profiling unless that collection is explicitly justified and approved.
* **Fail-soft and non-blocking:** Analytics collection must not break the product or degrade primary user flows.

## Types of Analytics

* Pageviews
* Source
* Links Followed
* Actions
* Usage
* Versions
* Heartbeats
* Traffic Snapshots
* Aggregate Counters
* User-Provided Data

## Note on Identifiers

TGC policy does **not** treat a hashed value as automatically anonymous or automatically acceptable.

A hashed identifier is still an identifier if it can be used to distinguish, recognize, correlate, or track a person, device, browser, install, or repeat actor over time. Because of that, **hashed IDs are prohibited by default** unless a product-specific policy explicitly approves them for a narrowly defined purpose.

The following are **not allowed by default**:

* Hashed IP uniqueness systems.
* Hashed device IDs.
* Hashed install IDs.
* Hashed account IDs used for silent analytics correlation.
* Any identifier used to create cross-day linking or behavioral profiling.

If a product ever requires an identifier, the document for that product must explicitly state:

* Why it is needed.
* What exact value is used.
* What it is used for.
* What it is **not** used for.
* Retention expectations.
* Why aggregate-only measurement is insufficient.

## Levels of Analytics

* Page Level
* Host Level
* App Level
* User Level
* Internal

---

## Level Definitions

### Page Level

Analytics built into webpage code. This captures signals from actual page execution in a browser or real page load. This is the closest thing to a confirmed physical page run, because the page code itself executes.

Typical examples:

* Pageviews
* Page-level actions
* Tracked link visits
* Referral source capture
* Client-side session signals
* Page heartbeats

### Host Level

Analytics from the hosting or edge layer, such as Cloudflare or similar platform reporting. This reflects **all observed traffic**, including:

* Human traffic
* Bots
* Crawlers
* Probes
* Malformed requests
* Blocked traffic

Host-level analytics are useful for traffic volume, infrastructure visibility, and broad traffic pattern awareness, but they are not the same as confirmed human usage.

### App Level

Analytics generated by code running inside an application itself. This is similar to page-level analytics in that it reflects code execution inside the product, but applies to software/app usage rather than public webpage loads.

Typical examples:

* App starts
* Version reports
* Feature usage
* Workflow actions
* Update checks
* Usage heartbeats
* Install or runtime diagnostics

### User Level

Information knowingly provided by the user or tied directly to the user relationship.

Examples may include:

* Email address
* Account identity
* Billing information
* User-submitted profile information
* Support-linked identifiers

This level requires the greatest restraint and should only be used where there is a direct business or product need.

### Internal

Analytics, logs, reporting, or instrumentation that exist strictly for internal TGC system use.

Examples:

* Admin diagnostics
* Internal dashboards
* System health signals
* Private operational reporting
* Internal automation traces

---

## Glossary

* **Analytics:** Structured collection of useful signals intended to support product, operational, and marketing decisions.
* **Signal:** A single meaningful recorded event, measurement, or observed state.
* **Pageview:** A recorded visit or load of a webpage.
* **Source:** The origin of traffic, visit, or discovery path. Examples may include referrer, campaign tag, direct entry, or tracked link origin.
* **Tracked Link:** A link intentionally structured to preserve source or campaign attribution. Tracked links are useful for measuring which post, page, channel, or campaign produced a visit or downstream action.
* **Counted Intent Route:** A route or endpoint intentionally designed to count a meaningful action, such as an explicit download initiation, update check, or similar operator-defined event. This is different from a passive read route. A counted intent route exists to represent deliberate action.
* **Non-Counted Public Read:** A public route used to read or hydrate information without incrementing an action counter. This distinction is important because public reads and actual user intent are not the same thing.
* **Link Followed:** A recorded event showing that a user activated or followed a link.
* **Action:** A meaningful user or system event beyond a passive view. Examples include button clicks, form submissions, downloads, purchases, or feature use.
* **Hashed / Anonymized ID:** A non-plain-text identifier used to recognize repeat patterns or continuity without storing raw personal identity directly.
* **Usage:** Observed interaction with a page, product, feature, or workflow over time.
* **Version:** A reported software or site version associated with an event, client, or runtime state.
* **Heartbeat:** A recurring signal that indicates an active session, live page, running app instance, or continuing use state.
* **Physical Run:** A practical term for a real execution of code on a real page or in a real app instance, as opposed to only server-side or edge-observed traffic.
* **Attribution:** The process of connecting a visit, action, or conversion to a source, campaign, post, or tracked path.
* **PII:** Personally identifiable information. This includes data that directly identifies a person or can reasonably be tied back to them.
* **Telemetry:** Operational or product instrumentation emitted by software or systems for measurement, monitoring, reporting, or analysis.

---

## Working Policy Direction

TGC analytics should default toward:

* Operational usefulness.
* Minimal necessary collection.
* Clear separation between host traffic and confirmed product/page execution.
* Cautious treatment of user-linked data.
* Explicit naming of what each analytics layer does and does not mean.
* Aggregate-only counting where full event logs are not needed.
* Privacy-first implementation.

## Company-Wide Baseline Policy

Unless a product-specific policy explicitly states otherwise, TGC analytics follows this baseline:

* Prefer aggregate counts over event logs.
* Avoid raw personal identifiers.
* Avoid raw IP retention.
* Avoid uniqueness systems built from IPs or hidden identifiers.
* Avoid behavioral profiling and cross-day identity linking.
* Keep analytics non-blocking and fail-soft.
* Distinguish passive reads from counted intent.
* Distinguish host-observed traffic from code-executed page/app signals.
* Only collect user-level data when there is a direct business or product reason.
* Keep implementation, public wording, and source-of-truth documents aligned.

---

## Governance Rules

### 1. Policy Precedence

Company-wide analytics policy applies by default. If a product needs to differ from this baseline, that difference must be explicit in the product’s own documentation or source of truth. Silence does not imply permission.

### 2. Minimum Collection Rule

If a question can be answered with aggregate counts, then user-level or event-level collection should not be introduced.

### 3. Counted Intent vs Public Read Rule

TGC must distinguish between:

* **Counted intent routes:** explicit actions that are meant to represent user intent.
* **Non-counted public reads:** public reads, hydration, manifest fetches, or raw asset access that are not equivalent to deliberate action.

Examples from current TGC systems include a counted download-initiation route, a non-counted public manifest read route, and raw asset delivery that does not itself imply counted intent.

### 4. Host Traffic Is Not Human Usage Rule

Host-level traffic includes bots, probes, crawlers, malformed requests, and blocked traffic. Host-level numbers are useful for infrastructure visibility and broad traffic awareness, but they must not be casually described as human usage, app usage, or confirmed user engagement.

### 5. Physical Execution Rule

Page-level and app-level analytics are closer to confirmed execution because code actually ran on a real page or in a real app instance. Even so, page/app execution still does not justify unnecessary user identification.

### 6. No Hidden Identity Systems Rule

TGC must not silently introduce persistent or semi-persistent identity systems for analytics purposes without explicit approval. This includes cookies, install IDs, device IDs, fingerprinting, account-linking for analytics, hashed uniqueness systems, or similar approaches when their real purpose is repeat-user recognition.

### 7. User-Level Data Restraint Rule

User-level data requires the highest restraint. User-provided information such as email, billing details, contact details, or support-linked data should only be collected when there is a direct product, service, support, compliance, or commercial need.

### 8. Fail-Soft Analytics Rule

Analytics must never become a dependency that breaks the primary user path. When analytics fail, the product or page should continue functioning normally wherever possible.

### 9. Honest Wording Rule

Public copy, internal docs, reporting language, and implementation should not contradict each other. If a site uses website analytics but an application does not, that distinction must be stated clearly.

### 10. Product Scope Rule

Not every TGC property needs every analytics layer. Each product or site should explicitly declare which analytics levels it uses.

*For example: BUS Core site may use multiple layers, including page, host, app-adjacent release/reporting surfaces, and internal analytics. Star Map may intentionally remain page-level only.* That is acceptable as long as the declared scope is explicit and truthful.

---

## Governance Matrix

| Level          | What it covers                             | Allowed by default                                                                | Not allowed by default                                                  | Notes                                         |
| :------------- | :----------------------------------------- | :-------------------------------------------------------------------------------- | :---------------------------------------------------------------------- | :-------------------------------------------- |
| **Host Level** | Hosting/edge/platform traffic              | requests, broad traffic summaries, bot-aware traffic patterns, edge errors        | user linkage, identity building, claiming host traffic equals humans    | includes all observed traffic, not just users |
| **Page Level** | Website code execution                     | pageviews, source attribution, tracked link visits, page actions, page heartbeats | email capture by stealth, fingerprinting, hidden persistent identifiers | closer to physical page execution             |
| **App Level**  | Application code execution                 | app starts, version reports, feature actions, usage heartbeats, update checks     | install IDs or device IDs unless explicitly approved                    | similar to page level but in product runtime  |
| **User Level** | User-provided or directly user-linked data | account/contact/billing/support data only when operationally needed               | speculative collection, analytics-only PII expansion                    | highest restraint level                       |
| **Internal**   | Private TGC ops and diagnostics            | admin reporting, dashboards, diagnostics, automation traces                       | externalized user profiling                                             | internal system use only                      |

---

## Open Items

* Define approved fields for each analytics level in a stricter field list.
* Define prohibited collection examples in a sharper appendix.
* Define dev/test exclusion standards company-wide.
* Define retention expectations per layer.
* Define standard wording across README, SOT, telemetry docs, privacy pages, and reporting.

---

# Appendix A: Active Property Registry & Contract

This section is the canonical source of truth for analytics declarations across all True Good Craft public properties. It sits strictly between the top-level TGC Analytics Policy and the technical implementation layers (Lighthouse and Agent Smith).

## 1. Shared Event Taxonomy

All properties must use these canonical names for cross-site comparable actions:

* `page_view`
* `outbound_click`
* `contact_click`
* `service_interest`

Any event not on this list must be declared as a site-specific Layer 5 Extension event.

## 2. Property Declarations

### A. BUS Core

* **Site Key:** `buscore`
* **Support Class:** `legacy_hybrid`
* **Analytics Levels:** Host Level, Page Level, selected App/Internal
* **Capability Layers:**

  * Layer 1 (Registry): Yes
  * Layer 2 (Event): Yes
  * Layer 3 (Traffic): Yes
  * Layer 4 (Identity): Yes
  * Layer 5 (Extension): Minimal
* **Report Expectations:** This is the richest site by design. It is expected to expose richer report sections, including full Traffic and Identity blocks.

### B. Star Map Generator

* **Site Key:** `star_map_generator`
* **Production Host:** `starmap.truegoodcraft.ca`
* **Support Class:** `event_only`
* **Analytics Levels:** Page Level
* **Capability Layers:**

  * Layer 1 (Registry): Yes
  * Layer 2 (Event): Yes
  * Layer 3 (Traffic): No
  * Layer 4 (Identity): No
  * Layer 5 (Extension): Yes
* **Extension Events:** `preview_generated`, `high_res_requested`, `payment_click`, `download_completed`, `error_preview`, `error_high_res`.
* **Report Expectations:** Requests and visits are unsupported by design. Identity remains null. Expected useful output includes `page_view`, extension events, top paths, and attribution lists (source/campaign/content/referrer).

### C. True Good Craft (TGC Site)

* **Site Key:** `tgc_site`
* **Support Class:** `event_only`
* **Capability Layers:**

  * Layer 1 (Registry): Yes
  * Layer 2 (Event): Yes
  * Layer 3 (Traffic): No
  * Layer 4 (Identity): No
  * Layer 5 (Extension): No active extensions currently
* **Report Expectations:** Event telemetry only. No traffic layer or identity layer.

## 3. Global Filter Rules

* **Production Default:** `production_only` defaults to `true`.
* **Suppression Contract:** `dev_mode` is the canonical cross-site suppression standard. It suppresses Lighthouse first-party telemetry and Cloudflare beacon logic.
* **Null Honesty:** Unsupported metrics must remain null or omitted by rule. Normalization does NOT mean manufacturing parity. Do not fake identity or traffic data to make reports look uniform.

## 4. Report View Families

Lighthouse is the single source of truth for reporting data. The `GET /report` endpoint supports four strict view families.

* **`view=site`**: The normalized, single-site reporting shape. Requires a valid `site_key`. Returns `{ view: "site", generated_at, scope, summary, traffic, events, identity, health }`.
* **`view=fleet`**: The normalized fleet-wide summary shape. Returns `{ view: "fleet", generated_at, sites }`.
* **`view=source_health`**: The telemetry-integrity shape, focused on dropped/invalid rates. Returns `{ view: "source_health", generated_at, sites }`.
* **Legacy Mode (Bare `/report`)**: The older, BUS Core-centric shape. Activated only when `view` is omitted or blank. Explicit `view=legacy` is strictly invalid.

## 5. Normative JSON Examples

The top-level schema for `view=site` is identical across all properties. The difference between a `legacy_hybrid` and an `event_only` site is dictated by `null` values and boolean flags inside the payload, not by a different JSON structure.

**Normative `view=site` Payload (Event-Only Example):**

```json
{
  "view": "site",
  "generated_at": "2026-04-10T12:00:00.000Z",
  "scope": {
    "site_key": "tgc_site",
    "support_class": "event_only",
    "production_only": true,
    "section_availability": {
      "summary": true,
      "traffic": false,
      "identity": false
    }
  },
  "summary": {
    "accepted_events_7d": 5,
    "pageviews_7d": null,
    "traffic_requests_7d": null,
    "traffic_visits_7d": null,
    "has_recent_signal": true
  },
  "events": {
    "accepted_events": 5,
    "unique_paths": 2,
    "by_event_name": [{ "event_name": "page_view", "events": 5 }],
    "top_paths": [{ "path": "/", "events": 5 }],
    "top_sources": [{ "source": "search", "events": 5 }],
    "top_campaigns": [],
    "top_contents": []
  },
  "identity": null,
  "health": {
    "included_events": 5,
    "excluded_non_production_host": 0,
    "cloudflare_traffic_enabled": false
  }
}
```

## 6. Field-Shape Table

Downstream consumers MUST strictly adhere to these TypeScript definitions when parsing Lighthouse output.

| Object.Field                      | Exact Data Type                                | Definition                                                 |
| :-------------------------------- | :--------------------------------------------- | :--------------------------------------------------------- |
| scope.support_class               | string                                         | The authoritative class (legacy_hybrid, event_only, etc.). |
| summary.accepted_events_7d        | number                                         | Total accepted events over 7 days.                         |
| summary.traffic_requests_7d       | number | null                                  | Null if traffic layer is unsupported.                      |
| health.included_events            | number                                         | Count of events surviving the production filter.           |
| health.cloudflare_traffic_enabled | boolean                                        | True if the CF traffic layer is active.                    |
| events.by_event_name              | Array<{ event_name: string; events: number }>  | Array of event names and their counts.                     |
| events.top_paths                  | Array<{ path: string; events: number }>        | Array of paths and their counts.                           |
| events.top_contents               | Array<{ utm_content: string; events: number }> | Array of contents and their counts.                        |

## 7. Filter Semantics

* **Production Filter:** When `scope.production_only = true`, all event breakdown queries (`accepted_events`, `unique_paths`, `by_event_name`, `top_paths`, etc.) append a strict host-matching SQL filter.
* **Excluded Host Metric:** `health.excluded_non_production_host` is computed independently. It counts events where `accepted = 1` but the URL host does not match the production string. It is not a simple inverse of `included_events`.
* **Empty States under Filtering:** If a site has accepted events, but all of them are filtered out by `production_only`, the numeric fields (like `accepted_events`) return `0`. The breakdown lists (like `top_paths`) return `[]` (empty arrays). They do not return `null`.

## 8. Null / Empty / Unsupported Rules

Consumers must respect the strict semantic differences between absent data and filtered data:

* **`null` (Unavailable):** The capability layer is disabled by design (e.g., `traffic_requests_7d` for an `event_only` site).
* **`0` (No Activity):** The layer is supported, and the metric is requested, but the resulting count is zero.
* **`[]` (Empty Scope):** The breakdown is supported, but no rows survived the current filter (e.g., all hits were from a staging host while `production_only=true`).

## 9. Consumer Obligations for Agent Smith

Agent Smith is a read-only formatting bridge. It MUST adhere to the following hard rules:

* **Trust Lighthouse Scope:** Smith MUST NOT derive `support_class` locally from the `site_key`. It must read and obey `scope.support_class` sent by Lighthouse.
* **Correct Array Parsing:** Smith MUST parse `by_event_name` as an array of objects (`[{event_name, events}]`), not as a flat object map.
* **Correct Count Keys:** Smith MUST read the `events` key for numeric counts in attribution lists (e.g., `top_paths`, `top_sources`). It MUST NOT look for `count` or `pageviews`.
* **Correct Summary Keys:** Smith MUST parse `traffic_requests_7d` and `traffic_visits_7d`. It MUST NOT look for legacy keys like `requests_7d`.
* **Legible Filtering:** Smith MUST explicitly state when Report scope is production-only and render the `excluded_non_production_host` value so operators know when data is filtered versus missing.
* **No Fake Parity:** Smith MUST NOT silence or fake empty arrays, and it MUST NOT label an explicit `[]` array as "unavailable".

---

# Appendix B: Star Map Website Analytics Implementation Map

This appendix maps the analytics implementation inside the Star Map Generator repository. It is implementation-focused and sits below the policy / registry layer.

## 1. Repo Summary

Analytics in the Star Map repo is implemented entirely in the browser frontend. A single class, `StarMapApp`, in `frontend/app.js` contains all telemetry logic. Events are emitted through `this.trackEvent(eventName)` at meaningful points in the user flow, including page load, preview success or failure, payment click, and download success or failure. Payloads are sent to Lighthouse through an endpoint constructed at runtime from `config.lighthouseBaseUrl`, using `sendBeacon()` with a `fetch({ keepalive: true })` fallback. Backend, Pages Functions, and edge proxy code do not implement analytics emission.

## 2. Analytics File Inventory

* `frontend/app.js`

  * Role: Primary and sole analytics implementation file.
  * Relevance: Contains `LIGHTHOUSE_SITE_KEY`, `trackEvent()`, `hasDevModeCookie()`, `getTelemetryEndpoint()`, `getDeviceType()`, `getUtmPayload()`, all telemetry call sites, and sendBeacon / fetch dispatch.

* `frontend/config.js`

  * Role: Runtime operator config.
  * Relevance: Supplies `lighthouseBaseUrl`, `testMode`, and `devMode`. `lighthouseBaseUrl` controls whether telemetry fires at all.

* `frontend/config.example.js`

  * Role: Template config.
  * Relevance: Shows canonical config shape and leaves `lighthouseBaseUrl` empty by default.

* `frontend/index.html`

  * Role: Single HTML entry page.
  * Relevance: Loads `config.js` then `app.js`, which establishes the analytics initialization order.

* `docs/TELEMETRY.md`

  * Role: Canonical repo telemetry contract.
  * Relevance: Defines event names, payload rules, suppression rules, endpoint shape, and attribution rules.

* `SOURCE_OF_TRUTH.md`

  * Role: Product / deployment source of truth.
  * Relevance: Repeats Lighthouse contract expectations, event names, `site_key`, and suppression rules.

* `TGC Analytics Policie.md`

  * Role: Company-wide analytics policy copy present in repo.
  * Relevance: Declares Star Map support class, capability layers, and expected report behavior.

* `keep/preview_live_app.js`

  * Role: Historical snapshot.
  * Relevance: No telemetry calls found; useful only as evidence that telemetry was added later.

* `keep/prod_live_app.js`

  * Role: Historical snapshot.
  * Relevance: No telemetry calls found; not current truth.

* `keep/preview_live_config.js`

  * Role: Historical config snapshot.
  * Relevance: Does not include Lighthouse telemetry configuration.

## 3. Initialization Path

1. `frontend/index.html` loads `config.js`.
2. `frontend/index.html` loads `app.js`.
3. `config.js` sets `window.STAR_MAP_CONFIG`.
4. `app.js` merges `DEFAULT_CONFIG` with `window.STAR_MAP_CONFIG` into `APP_CONFIG`.
5. `DOMContentLoaded` triggers `new StarMapApp(APP_CONFIG)` automatically.
6. Inside the constructor:

   * `pageViewSent` is initialized.
   * `telemetrySuppressedForPageLoad` is set from `hasDevModeCookie()`.
   * `trackEvent('page_view')` is called.
7. Telemetry only proceeds if:

   * the `dev_mode` cookie is absent for that page load, and
   * `getTelemetryEndpoint()` resolves a non-empty endpoint.

## 4. Event Emission Map

| Event Name           | File              | Trigger Condition                        | Classification         | Notes                           |
| :------------------- | :---------------- | :--------------------------------------- | :--------------------- | :------------------------------ |
| `page_view`          | `frontend/app.js` | Constructor on page load                 | Canonical shared event | dedup guarded by `pageViewSent` |
| `preview_generated`  | `frontend/app.js` | Preview generation succeeds              | Extension event        | page action                     |
| `error_preview`      | `frontend/app.js` | Preview generation fails                 | Extension event        | error event                     |
| `high_res_requested` | `frontend/app.js` | User initiates high-res path             | Extension event        | action event                    |
| `payment_click`      | `frontend/app.js` | User enters Stripe payment redirect path | Extension event        | outbound payment-related action |
| `download_completed` | `frontend/app.js` | Paid completion succeeds                 | Extension event        | action event                    |
| `download_completed` | `frontend/app.js` | Direct high-res download succeeds        | Extension event        | action event                    |
| `error_high_res`     | `frontend/app.js` | Paid completion fails                    | Extension event        | error event                     |
| `error_high_res`     | `frontend/app.js` | Direct high-res generation fails         | Extension event        | error event                     |

### Canonical shared events actually emitted in code

* `page_view`

### Canonical shared events declared in docs but not emitted in code

* `outbound_click`
* `contact_click`
* `service_interest`

### Site-specific extension events emitted in code

* `preview_generated`
* `error_preview`
* `high_res_requested`
* `payment_click`
* `download_completed`
* `error_high_res`

## 5. Payload Contract from Site to Lighthouse

### Endpoint pattern

`new URL('/metrics/event', lighthouseBaseUrl).toString()`

### Current configured endpoint observed in repo

`https://buscore-lighthouse.jamie-eb1.workers.dev/metrics/event`

### Transport

* Primary: `navigator.sendBeacon()`
* Fallback: `fetch(..., { method: 'POST', keepalive: true })`

### Payload shape

```json
{
  "type": "event",
  "site_key": "star_map_generator",
  "event_name": "<eventName>",
  "client_ts": "<ISO timestamp>",
  "path": "<window.location.pathname>",
  "url": "<window.location.href>",
  "referrer": "<document.referrer or empty string>",
  "utm": {
    "source": "<utm_source>",
    "medium": "<utm_medium>",
    "campaign": "<utm_campaign>",
    "term": "<utm_term>",
    "content": "<utm_content>"
  },
  "device": "<desktop|mobile|tablet>",
  "viewport": "<WxH>",
  "lang": "<navigator.language>",
  "tz": "<time zone>",
  "test_mode": false,
  "src": "<optional src query param>"
}
```

### Request behavior

* Body is JSON.
* `Content-Type: application/json` is set.
* No auth header is sent.
* No retries or batching are implemented.
* Failures are swallowed.
* Telemetry is explicitly non-blocking.

## 6. Attribution and Tracked-Link Handling

* `src` is read from the `?src=` query param and included only when non-empty.
* UTM fields are read from URL query params on every event dispatch.
* `document.referrer` is sent as-is.
* No tracked-link generation or link decoration is implemented in the repo.
* No `outbound_click` listener is implemented.
* Stripe redirect is represented by `payment_click`, not by `outbound_click`.
* Internal anchor navigation is not tracked.

## 7. Environment and Suppression Behavior

### Canonical suppression actually implemented

* Presence of a `dev_mode` cookie suppresses all telemetry for the current page load.
* Empty or invalid `lighthouseBaseUrl` suppresses telemetry by causing `getTelemetryEndpoint()` to return an empty string.

### Config flags

* `devMode` affects UI behavior only and does not suppress telemetry.
* `testMode` adds `test_mode: true` to the payload but does not suppress emission.

### Host behavior

* No host-based suppression is implemented in frontend code.
* Localhost can still emit telemetry if `lighthouseBaseUrl` is set.
* Cloudflare beacon suppression is referenced in policy language but no Cloudflare beacon script is present in this repo.

## 8. Product Declaration Extracted from Code

* Site: Star Map Generator
* Site key: `star_map_generator`
* Production host: `https://starmap.truegoodcraft.ca`
* Support class: `event_only`
* Analytics levels used: Page Level only
* Canonical events emitted: `page_view`
* Extension events emitted: `preview_generated`, `error_preview`, `high_res_requested`, `payment_click`, `download_completed`, `error_high_res`
* Traffic layer in repo: not found
* Identity layer in repo: not found
* Internal-only reporting hooks in repo: not found

## 9. Lighthouse Assumptions Made by This Repo

1. Lighthouse exposes `POST /metrics/event`.
2. No auth header is required from the frontend.
3. Origin whitelisting is enforced Lighthouse-side.
4. `star_map_generator` is a registered property in Lighthouse.
5. `test_mode` events are excluded from production reports Lighthouse-side.
6. Non-production host segregation is handled Lighthouse-side.
7. Lighthouse accepts the nested `utm` object and optional `src` field.
8. Event delivery is effectively fire-and-forget.

## 10. Exact Flow Map

1. Browser loads Star Map page.
2. `frontend/index.html` loads `config.js` then `app.js`.
3. `config.js` defines `window.STAR_MAP_CONFIG`.
4. `app.js` builds `APP_CONFIG`.
5. `DOMContentLoaded` constructs `StarMapApp`.
6. Constructor checks `dev_mode` cookie.
7. Constructor emits `page_view`.
8. `trackEvent()` builds the payload from page state, URL params, and config.
9. Payload is sent to Lighthouse via `sendBeacon()` or `fetch(... keepalive: true)`.
10. Preview, payment, and download paths emit additional extension events through the same transport.
11. Errors are swallowed and telemetry does not block the user flow.

## 11. Confirmed Contract Drift / Mismatch

### Confirmed matches

* `site_key` matches Star Map declaration.
* Event names implemented in code match approved names in repo docs.
* `type: 'event'`, ISO timestamp, nested `utm`, optional `src`, `sendBeacon` primary, and non-blocking behavior all match repo telemetry documentation.
* Identity fields are absent, consistent with `event_only`.

### Confirmed mismatches or gaps

1. `frontend/config.js` hardcodes a Lighthouse base URL while repo docs describe this as operator-supplied / TBD.
2. `outbound_click`, `contact_click`, and `service_interest` are declared in shared taxonomy but are not emitted by current code.
3. Cloudflare beacon suppression is referenced in company policy language but no Cloudflare beacon implementation exists in this repo.
4. No automatic `test_mode=true` protection exists for localhost; emission depends on operator config discipline.

### Items requiring Lighthouse repo inspection

* Whether the configured Lighthouse host is production or dev.
* Whether `star_map_generator` is registered in Lighthouse.
* Whether production origin whitelisting is active.
* Whether `test_mode` exclusion and host segregation are working as assumed.

## 12. Compact Extract for Registry / Mapping

* Site: Star Map Generator
* Site key: `star_map_generator`
* Production host: `https://starmap.truegoodcraft.ca`
* Analytics level(s): Page Level only
* Support class: `event_only`
* Canonical events: `page_view` emitted; `outbound_click`, `contact_click`, `service_interest` declared but not emitted
* Extension events: `preview_generated`, `error_preview`, `high_res_requested`, `payment_click`, `download_completed`, `error_high_res`
* Analytics entry files: `frontend/index.html`, `frontend/config.js`, `frontend/app.js`
* Emission files: `frontend/app.js`
* Payload endpoint: `POST /metrics/event` on configured Lighthouse base URL
* Suppression rules: `dev_mode` cookie suppresses; empty `lighthouseBaseUrl` suppresses; `devMode` does not suppress telemetry
* Attribution fields observed: `src`, nested UTM fields, `referrer`
* Known Lighthouse assumptions: endpoint path, registration, origin whitelist, test-mode exclusion, host segregation, fire-and-forget delivery
* Known contract drift: hardcoded base URL, declared-but-unemitted canonical events, Cloudflare beacon mismatch, localhost `test_mode` discipline gap
* Unknowns requiring Lighthouse inspection: registration, whitelist, exclusion behavior, host filtering behavior

---

# Appendix C: Lighthouse Analytics Ingestion, Storage, and Reporting Map

This appendix maps the Lighthouse repository as the central analytics ingestion and reporting system.

## 1. Repo Summary

Lighthouse is implemented as a single Cloudflare Worker in `src/index.ts`. That file contains route handling, ingestion logic, validation, normalization, site registry, rate limiting, storage writes, query logic, support-class behavior, and report assembly. Telemetry ingress currently has two POST routes: legacy BUS Core pageviews on `/metrics/pageview` and canonical multi-site events on `/metrics/event`. Reporting is exposed through an admin-token-protected `/report` endpoint that supports legacy bare mode, `view=fleet`, `view=site`, and `view=source_health`. Storage is primarily Cloudflare D1, with R2 used for manifest and release artifacts rather than event storage.

## 2. Analytics File Inventory

* `src/index.ts`

  * Role: Full Lighthouse worker implementation.
  * Relevance: Contains ingress endpoints, validation, normalization, registry declarations, D1 insert/query logic, traffic capture logic, support-class rules, and report assembly.

* `migrations/0001_init.sql`

  * Role: Base metrics schema.
  * Relevance: Defines `metrics_daily` used by legacy metrics/report sections.

* `migrations/0002_add_calculations.sql`

  * Role: Historical schema update.
  * Relevance: Adds calculations column; part of schema history.

* `migrations/0003_add_buscore_traffic_daily.sql`

  * Role: Creates BUS Core Cloudflare traffic table.
  * Relevance: Base traffic storage for requests/visits capture.

* `migrations/0004_rename_buscore_traffic_pageviews_to_requests.sql`

  * Role: Renames traffic semantics.
  * Relevance: Establishes `requests` terminology used in current reporting.

* `migrations/0005_add_pageview_ingestion.sql`

  * Role: Legacy pageview ingestion schema.
  * Relevance: Defines `pageview_events_raw`, `pageview_daily`, `pageview_daily_dim`, and `pageview_rate_limit`.

* `migrations/0006_add_anonymous_continuity.sql`

  * Role: Identity continuity schema change.
  * Relevance: Adds anonymous continuity fields to legacy pageview raw storage.

* `migrations/0007_add_site_events.sql`

  * Role: Multi-site event schema.
  * Relevance: Defines `site_events_raw` for `/metrics/event`.

* `migrations/0008_add_site_event_rate_limit.sql`

  * Role: Site-event rate limit schema.
  * Relevance: Defines `site_event_rate_limit` used for minute-bucket IP-hash throttling.

* `wrangler.toml`

  * Role: Worker config and bindings.
  * Relevance: Declares D1, R2, and cron schedule.

* `README.md`

  * Role: Operator-facing contract doc.
  * Relevance: Documents endpoints, report shapes, support classes, capability layers, and expected Star Map behavior.

* `SOT.md`

  * Role: Lighthouse source-of-truth doc.
  * Relevance: Documents intended ingress, filter, storage, and report behavior.

* `CHANGELOG.md`

  * Role: Behavior history.
  * Relevance: Confirms rollout of report views, filter fixes, and contract changes.

* `TGC Analytics Policie.md`

  * Role: Overlay policy/contract copy.
  * Relevance: Includes consumer obligations and property registry language to compare against implementation.

* `plan.md`

  * Role: Alignment/planning note.
  * Relevance: Describes intended guardrails but is not runtime authority.

* `tests/report.test.mjs`

  * Role: Report-shape tests.
  * Relevance: Confirms expected fields, support-class behavior, and null/0/[] semantics.

* `tests/identity.test.mjs`

  * Role: Identity helper tests.
  * Relevance: Confirms UUID normalization and identity metric handling.

* `package.json`

  * Role: Repo metadata.
  * Relevance: Confirms test/build setup and current package version.

## 3. Ingress Endpoint Map

### `/metrics/pageview`

* Method(s): `POST`, `OPTIONS`
* Purpose: Legacy BUS Core pageview ingestion.
* Expected request: canonical pageview JSON with `type = pageview` plus timestamp, path, URL, referrer, device, viewport, lang, tz, and `utm` object.
* Response: `204 No Content` for POST, `200` for OPTIONS.
* Audience: browser-facing.
* Auth/origin: no auth; CORS allow-origin only for BUS Core allowed origins.

### `/metrics/event`

* Method(s): `POST`, `OPTIONS`
* Purpose: Canonical multi-site event ingestion.
* Expected request: canonical event JSON with `site_key`, `event_name`, timestamp, path, URL, referrer, device, viewport, lang, tz, and `utm` object; optional `src`, `anon_user_id`, `session_id`, `is_new_user`, `event_value`, `test_mode`.
* Response: `204 No Content` for POST, `200` for OPTIONS.
* Audience: browser-facing.
* Auth/origin: no auth; CORS allow-origin only for active site allowed origins.

### `/report`

* Method(s): `GET`
* Purpose: internal/admin reporting endpoint.
* Expected request: `X-Admin-Token` header and optional query params `view`, `site_key`, `exclude_test_mode`, `production_only`.
* Response: JSON report payloads; `401` unauthorized, `400` invalid request variants, or `503` when report unavailable.
* Audience: internal/operator.
* Auth/origin: exact token match against `env.ADMIN_TOKEN`; no origin-based restriction.

### Other analytics-adjacent routes

* `/update/check`: public counted update-check route.
* `/download/latest`: public counted download-initiation route.
* `/manifest/core/stable.json`: public non-counted manifest read route.
* `/releases/:filename`: public release artifact fetch route.

## 4. Event Ingestion Path for Canonical Multi-Site Events

1. Request enters the Worker and matches `POST /metrics/event`.
2. Worker builds request context, including origin, content-type, user-agent, request id, and Cloudflare request metadata.
3. Worker reads the request body and returns `204` immediately while deferring deeper processing via `waitUntil`.
4. Async ingest computes server-side receive timestamps and hashes for client IP and user agent when available.
5. Body is parsed from raw text into JSON.
6. Payload is normalized and validated using the canonical event parser.
7. `site_key` is validated against the code-defined Lighthouse registry.
8. If an IP hash exists, minute-bucket rate limiting is applied through `site_event_rate_limit`.
9. A normalized/enriched row is inserted into `site_events_raw` with `accepted = 1` for accepted events or `accepted = 0` and `drop_reason = rate_limited` for throttled events.
10. Client always sees only the earlier `204`; invalid or rejected details are not returned in response bodies.

## 5. Registry and Property Declaration Map

Lighthouse knows what a tracked property is through a code-defined registry in `src/index.ts`.

### Registry source

* Symbol: `TRACKED_SITES`
* Declares per-site fields including:

  * `site_key`
  * label
  * status
  * `production_hosts`
  * `allowed_origins`
  * `staging_hosts`
  * `cloudflare_traffic_enabled`
  * `cloudflare_host`
  * `production_only_default`

### Validation/enforcement points

* `site_key` is validated at `/metrics/event` ingest.
* `site_key` is validated again for report views that require it.
* Unknown site keys are silently rejected at ingest but produce `400 invalid_site_key` at `/report`.

### support_class source and enforcement

* `supportClassForSite()` maps a site to runtime support class.
* `sectionAvailabilityForSupportClass()` controls section availability.
* BUS Core is hard-coded into `legacy_hybrid` behavior.
* Non-BUS Core active non-Cloudflare sites become `event_only`.

### Shared taxonomy and extension handling

* Shared taxonomy list exists in code.
* Alias normalization exists for reporting (`pageview -> page_view`, `link_click -> outbound_click`).
* Ingest does not restrict event names to shared taxonomy; non-empty event names can be accepted as extension events.

## 6. Validation and Normalization Map

### Required canonical event fields

* `site_key`
* `event_name`
* `client_ts`
* `path`
* `url`
* `referrer`
* `device`
* `viewport`
* `lang`
* `tz`
* `utm` object

### Required legacy pageview fields

* `type = pageview`
* `client_ts`
* `path`
* `url`
* `referrer`
* `device`
* `viewport`
* `lang`
* `tz`
* `utm` object

### Core validation rules

* timestamps must be parseable dates
* `path` must start with `/`
* `url` must be absolute
* device must be one of allowed device values
* viewport must match numeric `WxH` pattern
* `site_key` must exist in Lighthouse registry for canonical events
* `event_name` must be a non-empty trimmed string

### Optional/coerced fields

* `src` and UTM subfields become null if blank
* `anon_user_id` and `session_id` are normalized only if UUID-shaped
* `is_new_user` and `test_mode` are coerced into integer-like booleans
* `event_value` is optional on canonical events

### Unknown field behavior

Unknown fields are ignored rather than rejected.

### Important asymmetry

* Invalid legacy pageviews are persisted as dropped-invalid evidence.
* Invalid canonical `/metrics/event` payloads are dropped silently and not persisted as invalid rows.

## 7. Storage Map

### Storage technologies

* Primary analytics storage: Cloudflare D1
* Non-analytics storage: R2 for manifests/releases
* No analytics KV usage found

### D1 tables used

* `metrics_daily`
* `buscore_traffic_daily`
* `pageview_events_raw`
* `pageview_daily`
* `pageview_daily_dim`
* `pageview_rate_limit`
* `site_events_raw`
* `site_event_rate_limit`

### Insert paths

* Legacy pageview raw insert → `pageview_events_raw`
* Legacy pageview aggregate upsert → `pageview_daily`
* Legacy pageview dimension rollups → `pageview_daily_dim`
* Canonical event raw insert → `site_events_raw`
* Rate limit minute buckets → `pageview_rate_limit` / `site_event_rate_limit`
* Cloudflare traffic snapshots → `buscore_traffic_daily`

### Raw vs aggregate

* Legacy pageview path has both raw and aggregate forms.
* Canonical multi-site events currently appear raw-only in `site_events_raw`.

### Drop/reject tracking

* Legacy pageview invalid and rate-limited events are tracked.
* Canonical event rate-limited rows are tracked.
* Canonical invalid-body/schema events are not persisted.

### Retention/cleanup

* `prunePageviewData()` deletes old legacy pageview raw data and old rate-limit buckets.
* No retention deletion for `site_events_raw` was confirmed in inspected code.
* No retention deletion for `buscore_traffic_daily` was confirmed in inspected code.

## 8. Production Filtering and Suppression Logic

### Production host filtering

* Production hosts come from registry `production_hosts`.
* SQL filter clauses are built from those hosts and applied to event report queries when `production_only = true`.
* `excluded_non_production_host` is computed independently from this filter logic.

### Accepted vs rejected vs excluded

* Accepted: stored with `accepted = 1`
* Rejected by rate limit: stored with `accepted = 0` and `drop_reason = rate_limited`
* Excluded from reports: accepted rows filtered out by production host and/or `test_mode` filters

### `test_mode` handling

* Stored as integer flag.
* Excluded from reporting when `exclude_test_mode = true`.
* `excluded_test_mode` is still reported in observability sections.

### Unsupported traffic and identity layers

* Traffic is null when Cloudflare traffic is unsupported.
* Identity is null for support classes that do not support it.
* Event-only sites remain event-only at the report layer through feature gating.

### Null / 0 / [] semantics

* `null` = unsupported by design
* `0` = supported metric but zero result
* `[]` = supported breakdown but no rows survived the filters

### Not found server-side

No server-side `dev_mode` cookie suppression logic was found. That suppression is site-side, not Lighthouse-side.

## 9. Reporting Pipeline Map

### `/report` view resolution

* blank or omitted `view` → legacy report mode
* `view=fleet` → fleet summary
* `view=site` → normalized per-site report
* `view=source_health` → telemetry integrity / observability view
* explicit `view=legacy` is invalid

### `view=site`

Built by the per-site report builder and composed from:

* `scope`
* `summary`
* `traffic`
* `events`
* `identity`
* `health`

### `view=fleet`

Built as one site entry per tracked property, with support-class-aware signal summaries.

### `view=source_health`

Built as one site entry per property focused on accepted signal counts and drop/exclusion observability.

### Legacy bare `/report`

Built through legacy BUS Core–centric report assembly and may optionally include scoped site-events when a valid `site_key` is provided.

### How sections are built

* `scope`: registry + filter flags + support class + section availability
* `summary`: accepted event totals, optional pageview totals, optional traffic totals, last received time, recent signal state
* `traffic`: Cloudflare-based totals/latest capture when supported
* `events`: grouped canonical site-event queries
* `identity`: legacy pageview identity section when supported
* `health`: observability and drop/exclusion rollups

## 10. Query / Aggregation Map

### Canonical event aggregations

* `accepted_events_7d`: count from `site_events_raw` with accepted/test/production filters
* `top_paths`: grouped by `path`
* `by_event_name`: grouped by `event_name`, then alias-normalized into canonical shared names for reporting
* `top_sources`: row-level attribution precedence of `src -> utm_source -> referrer class -> (direct)` then grouped in memory
* `top_campaigns`: grouped by `utm_campaign`
* `top_contents`: grouped by `utm_content`
* `top_referrers`: grouped by `referrer_domain`
* `included_events`: observability count of accepted rows surviving report filters
* `excluded_non_production_host`: observability count of accepted rows outside production host filter

### Traffic aggregations

* `traffic_requests_7d`: summed from `buscore_traffic_daily`
* `traffic_visits_7d`: summed from `buscore_traffic_daily`
* `cloudflare_traffic_enabled`: registry-derived capability flag

### Legacy pageview aggregations

* legacy top paths / top referrers from `pageview_daily_dim`
* legacy observability counters from `pageview_daily`
* legacy identity metrics from `pageview_events_raw`

## 11. Contract Truth vs Docs Map

### Confirmed by code

* `/metrics/pageview` and `/metrics/event` both return `204` and process asynchronously.
* `/report` is admin-token protected.
* `fleet`, `site`, `source_health`, and bare legacy modes exist.
* Event-only sites return null traffic/identity by design.

### Documented but not found in code

* No server-side `dev_mode` suppression exists.
* Agent Smith obligations are documented but not runtime-enforced in Lighthouse.

### Implemented but under-documented or asymmetric

* Explicit `view=legacy` is invalid; only blank/omitted view maps to legacy.
* Invalid `/metrics/event` payloads are dropped without invalid-row persistence.
* Ingest `site_key` validation checks registry existence, not active status, while browser CORS allow-lists are active-only.

### Drift candidates

* README schema documentation appears incomplete relative to active event tables.
* Some docs describe logical sections differently than the actual `view=site` runtime payload.
* Some policy wording describes production-only defaults globally, while runtime behavior is site-specific.

## 12. Agent Smith Consumer Contract

The Lighthouse runtime fields that downstream consumers must parse include:

* `scope.support_class`
* `scope.production_only`
* `scope.section_availability`
* `summary.traffic_requests_7d`
* `summary.traffic_visits_7d`
* `events.by_event_name[]` as an array of objects
* `events.top_paths[]` with `{ path, events }`
* `events.top_sources[]` with `{ source, events }`
* `events.top_campaigns[]` with `{ utm_campaign, events }`
* `events.top_referrers[]` with `{ referrer_domain, events }`
* `events.top_contents[]` with `{ utm_content, events }`
* `health.included_events`
* `health.excluded_non_production_host`

The repo documents Smith obligations around those shapes, but Lighthouse itself does not enforce Smith parsing behavior.

## 13. Exact Flow: Star Map `page_view` Through Lighthouse

1. Star Map browser sends `POST /metrics/event` with `site_key = star_map_generator` and `event_name = page_view`.
2. Lighthouse route matches `/metrics/event` and returns `204` immediately.
3. CORS allow-origin is only granted for active allowed origins, including Star Map production origin.
4. Async processing validates required fields and checks that `star_map_generator` exists in the registry.
5. Event is normalized and enriched with server-side receive metadata.
6. Rate limit is checked using site-event minute bucket logic.
7. Accepted row is written into `site_events_raw`.
8. Later, `/report?view=site&site_key=star_map_generator` resolves into per-site report mode.
9. Filter defaults include `exclude_test_mode = true` and `production_only = true` for Star Map.
10. Report queries run against `site_events_raw` with accepted/test/production filters.
11. `scope.support_class` resolves to `event_only`.
12. `summary.accepted_events_7d` is populated from included site-event rows.
13. `traffic` and `identity` remain null/unsupported for Star Map.
14. `events` and `health` are assembled and returned in the normalized site-report payload.

## 14. Confirmed Contract Alignment / Drift Relative to Star Map

### Confirmed matches

* Star Map is registered under `star_map_generator`.
* Star Map production host/origin is recognized.
* Star Map defaults to `production_only = true`.
* Star Map resolves to `event_only` support class.
* Event-only output nullability for traffic and identity matches Star Map assumptions.

### Confirmed mismatches / drift

* Policy/docs language around global production-only defaults is looser than runtime truth, which is site-specific.
* Some repo docs under-describe active event tables and runtime output shapes.

### Unknowns requiring Smith-side inspection

* Whether Smith actually parses array/object shapes correctly.
* Whether Smith reads `events` count keys instead of legacy assumptions.
* Whether Smith renders null / 0 / [] semantics and production filtering context correctly.

## 15. Compact Extract for Registry / Mapping

* Repo: Lighthouse
* Ingress endpoints: `/metrics/pageview`, `/metrics/event`
* Report endpoint: `/report` with legacy, fleet, site, and source_health modes
* Registry source: `TRACKED_SITES` in `src/index.ts`
* Validation source: canonical pageview/event parsers in `src/index.ts`
* Storage layer: Cloudflare D1
* Storage objects: `metrics_daily`, `buscore_traffic_daily`, `pageview_events_raw`, `pageview_daily`, `pageview_daily_dim`, `pageview_rate_limit`, `site_events_raw`, `site_event_rate_limit`
* support_class enforcement: runtime support-class and section-availability helpers in `src/index.ts`
* production filter enforcement: report filter normalization plus SQL host-clause builders
* test_mode handling: stored at ingest and excluded by report filters by default
* event acceptance/rejection: validate → optional rate limit → raw insert; invalid `/metrics/event` dropped silently, invalid legacy pageview persisted as dropped-invalid
* report shape source: report assembly functions in `src/index.ts`
* null / 0 / [] semantics source: support-class gating + SQL aggregates + empty query results
* Smith-facing contract fields: support class, summary traffic keys, event arrays, health inclusion/exclusion fields
* Known Star Map matches: site registration, production defaults, event-only behavior, production filtering
* Known Star Map mismatches: mostly doc drift rather than confirmed runtime mismatch
* Unknowns requiring Smith inspection: parser compliance, key-name assumptions, rendering behavior

---

# Appendix D: Agent Smith Lighthouse Consumption and Rendering Map

This appendix maps how Agent Smith consumes Lighthouse output, interprets it, and renders it into operator-facing reports.

## 1. Repo Summary

Agent Smith is a Cloudflare Workers application that receives Discord slash-command interactions, routes them through a singleton Durable Object, and for `/report` calls fetches telemetry from a single configurable Lighthouse backend URL. Smith is strictly read-only in the analytics path: it does not mutate Lighthouse state. Report interpretation is deterministic and code-driven. Smith supports four Lighthouse payload families: legacy bare mode, `fleet`, `site`, and `source_health`. Payload normalization is centralized in typed helper functions rather than raw JSON access. A major current truth is that support-class behavior is still derived locally from hardcoded `siteKey` checks instead of being read from a Lighthouse `scope.support_class` field.

## 2. Analytics File Inventory

* `src/commands/report.ts`

  * Role: Discord `/report` command handler.
  * Relevance: Parses operator request options, builds Lighthouse request mode, calls Lighthouse client, formats final output, handles failures.

* `src/services/lighthouse.ts`

  * Role: Lighthouse HTTP client.
  * Relevance: Builds report URL, injects `X-Admin-Token`, fetches Lighthouse, parses JSON, normalizes payload, throws typed errors.

* `src/types/telemetry.ts`

  * Role: Lighthouse payload type/normalization layer.
  * Relevance: Defines report interfaces and all normalization functions for legacy, fleet, site, and source_health payload families. Also contains alias-resolution helpers.

* `src/logic/report.ts`

  * Role: Formatting and operator read-line generation.
  * Relevance: Contains report-format dispatchers and per-section rendering logic for legacy, fleet, site, and source_health outputs.

* `src/types.ts`

  * Role: Environment typing.
  * Relevance: Declares `LIGHTHOUSE_REPORT_URL` and `LIGHTHOUSE_ADMIN_TOKEN` used by the Lighthouse client.

* `wrangler.toml`

  * Role: Worker configuration.
  * Relevance: Defines default `LIGHTHOUSE_REPORT_URL` value.

* `CONTRACTS.md`

  * Role: Operator contract doc.
  * Relevance: Documents expected route behavior, field aliases, and output expectations.

* `tests/report_contract.test.ts`

  * Role: TypeScript contract tests.
  * Relevance: Confirms alias handling, payload family normalization, null-honesty behavior, and truncation.

* `tests/test_report_logic.py`

  * Role: Legacy Python analytics logic test.
  * Relevance: Tests deprecated logic rather than the active TypeScript path.

* `tests/test_report_route_policy.py`

  * Role: Legacy Python route policy test.
  * Relevance: Also targets deprecated Python-era code rather than the active TypeScript path.

## 3. Lighthouse Request Map

### Fleet request

* Mode: default `/report` with no site specified
* Request: `GET <LIGHTHOUSE_REPORT_URL>?view=fleet`
* Headers: `X-Admin-Token`
* Expected response family: fleet

### BUS Core legacy request

* Mode: `/report site:buscore`
* Request: `GET <LIGHTHOUSE_REPORT_URL>` with no `view` query param
* Headers: `X-Admin-Token`
* Expected response family: legacy bare report

### One-site normalized request

* Mode: `/report site:star_map_generator` or `/report site:tgc_site`
* Request: `GET <LIGHTHOUSE_REPORT_URL>?view=site&site_key=<siteKey>`
* Headers: `X-Admin-Token`
* Expected response family: site

### Source health request

* Mode: `/report view:source_health`
* Request: `GET <LIGHTHOUSE_REPORT_URL>?view=source_health`
* Headers: `X-Admin-Token`
* Expected response family: source_health

## 4. Report Fetch Flow

1. Discord sends a signed interaction to the Worker.
2. Signature verification is performed.
3. Interaction is forwarded to the singleton Durable Object.
4. `/report` command handler parses the operator request into a `LighthouseReportRequest`.
5. Lighthouse client builds the report URL from `LIGHTHOUSE_REPORT_URL` plus query params.
6. `X-Admin-Token` is injected into the request headers.
7. Smith performs `fetch()` to Lighthouse.
8. Response JSON is parsed.
9. Payload is normalized into one of the supported report families.
10. Formatter dispatches to the correct rendering function.
11. Final text is clamped to Discord message length.
12. Response is returned to Discord.

## 5. Lighthouse Contract Fields Actually Used by Smith

### Scope fields used

* `scope.site_key`
* `scope.label`
* `scope.backend_source`
* `scope.cloudflare_traffic_enabled` (alias-handled)
* `scope.production_only`
* `scope.section_availability`

### Summary fields used

* `summary.pageviews_7d`
* `summary.requests_7d`
* `summary.visits_7d`
* `summary.accepted_events_7d` (alias-handled from `accepted_signal_7d` or `accepted_events_7d`)
* `summary.last_received_at` (alias-handled)
* `summary.has_recent_signal`

### Traffic fields used

* `traffic.cloudflare_traffic_enabled`
* `traffic.latest_day.day`
* `traffic.latest_day.visits`
* `traffic.latest_day.requests`
* `traffic.latest_day.captured_at`
* `traffic.last_7_days.visits`
* `traffic.last_7_days.requests`
* `traffic.last_7_days.avg_daily_visits`
* `traffic.last_7_days.avg_daily_requests`
* `traffic.last_7_days.days_with_data`

### Event fields used

* `events.accepted_signal_7d` / `events.accepted_events`
* `events.has_recent_signal`
* `events.last_received_at`
* `events.unique_paths`
* `events.top_paths[]`
* `events.by_event_name`
* `events.top_sources[]`
* `events.top_campaigns[]`
* `events.top_referrers[]`
* `events.top_contents[]`

### Identity fields used

* `identity.today.new_users`
* `identity.today.returning_users`
* `identity.today.sessions`
* `identity.last_7_days.new_users`
* `identity.last_7_days.returning_users`
* `identity.last_7_days.sessions`
* `identity.last_7_days.return_rate`
* `identity.top_sources_by_returning_users[]`

### Health fields used

* `health.dropped_invalid`
* `health.dropped_rate_limited`
* `health.last_received_at`
* `health.included_events`
* `health.excluded_test_mode`
* `health.excluded_non_production_host`
* `health.cloudflare_traffic_enabled`
* `health.production_only_default`

## 6. Parsing and Shape Assumptions

* `by_event_name` is treated as an object map, not an array.
* Top lists accept a label from alternate keys such as `name` or source-specific keys and prefer `count`, with some fallback to `pageviews`.
* `top_paths` accepts both `count` and `pageviews`.
* Null and undefined are both rendered as unavailable.
* Numeric zero is preserved as a real zero.
* Empty arrays are treated differently from missing fields when formatting attribution and event breakdowns.
* `section_availability` is stored, but only some keys are acted on.
* Smith does **not** read `scope.support_class` from Lighthouse.
* Support class is still derived locally from hardcoded site keys.

## 7. Formatting / Rendering Map

For normalized site reports, Smith renders sections in this general order:

1. header and generated time
2. Summary
3. Event Telemetry
4. Attribution
5. Observability
6. Today (when meaningful traffic day data exists)
7. Traffic
8. Identity
9. Read

Important rendering behaviors:

* production-only scope is narrated explicitly when known
* `excluded_non_production_host` is surfaced in production-only attribution fallbacks
* unsupported sections are sometimes inferred from local heuristics, not solely from Lighthouse scope
* explicit empty lists are rendered as empty-in-scope rather than unavailable
* message length is clamped to Discord limits with a deterministic truncation suffix

## 8. support_class and Capability Behavior

Current Smith truth:

* `buscore` → locally treated as `legacy_hybrid`
* `star_map_generator` → locally treated as `event_only`
* `tgc_site` → locally treated as `event_only`
* unknown site keys → no local support class

Consequences:

* Smith does not automatically adapt if Lighthouse changes support class for a site.
* Smith does not automatically adapt to new sites unless local hardcoded logic is updated.
* `section_availability.traffic` and `section_availability.identity` influence output, but `section_availability.events` is not acted on.
* documented classes like `event_plus_cf_traffic` are not handled explicitly in current local support-class derivation.

## 9. Error and Fallback Behavior

* Network failure to Lighthouse produces a generic report retrieval failure.
* HTTP `400` with a recognized Lighthouse error payload maps to operator-specific error text.
* HTTP `401` and `503` fall through to generic failure handling.
* Invalid JSON produces a Lighthouse JSON failure path.
* Payload normalization failure produces a validation failure path.
* Formatting exceptions produce a formatting failure path.
* Optional invalid sections may be sanitized away rather than rejecting the whole payload.

## 10. Contract Truth vs Lighthouse Map

### Confirmed matches

* alias handling for `accepted_signal_7d`, `last_received_at`, and Cloudflare traffic flags
* fleet default route behavior
* BUS Core legacy route behavior
* normalized one-site route behavior
* source_health route behavior
* null-as-unavailable handling
* empty array versus missing field distinction
* Discord truncation behavior
* production-only narration and excluded-host messaging

### Confirmed mismatches / gaps

1. Smith does not read support class from Lighthouse and instead hardcodes it locally.
2. `section_availability.events` is not honored in formatting.
3. hardcoded Star Map extension-event messaging can drift from Lighthouse truth.
4. no special handling for `401`/`503` Lighthouse responses.
5. some Python tests in repo target deprecated code rather than the active TypeScript analytics path.

## 11. Final System-Level Read

With Appendix D added, the analytics system map is now closed end-to-end:

* website/property emits telemetry
* Lighthouse ingests, validates, stores, filters, and reports it
* Agent Smith fetches, normalizes, and renders it for operators

The main remaining systemic weakness is not missing documentation anymore. It is **contract drift risk** between:

* local site code,
* Lighthouse runtime truth,
* Smith local assumptions.

The most important current drift risk is Smith’s local support-class derivation instead of trusting Lighthouse scope.

## 12. Completion Snapshot

* Policy / glossary: present
* Property registry / contract: present
* Star Map repo implementation map: present
* Lighthouse ingestion / storage / reporting map: present
* Agent Smith consumption / rendering map: present

This canvas now functions as the current TGC analytics policy and system map baseline.
